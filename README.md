# Hysteria2 Config Bot

Telegram-бот для генерации YAML-конфигурационных файлов Hysteria2/Mihomo.

## Структура проекта

```
hysconfigbot/
├── cmd/
│   └── bot/
│       └── main.go          # Точка входа
├── internal/
│   └── handlers/
│       └── handlers.go      # Обработчики команд бота
├── pkg/
│   ├── client/
│   │   └── client.go        # HTTP-клиент с прокси
│   ├── consts/
│   │   └── consts.go        # Константы и сообщения
│   ├── generator/
│   │   └── generator.go     # Генераторы пароля и конфига
│   └── models/
│       └── models.go        # Модели данных
├── configs/
│   └── .env.example         # Пример конфигурации
├── .env                     # Конфигурация (не в git)
├── .gitignore
├── go.mod
├── go.sum
└── README.md
```

## Установка

```bash
# Клонируйте репозиторий
cd hysconfigbot

# Установите зависимости
go mod tidy

# Скопируйте пример конфигурации
cp configs/.env.example .env

# Отредактируйте .env, добавив токен бота
```

## Настройка

### 1. Получение токена бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен в `.env`

### 2. Настройка прокси (если требуется)

Для России и других стран с блокировкой Telegram:

**NekoBox:**
```env
SOCKS5_PROXY=127.0.0.1:2080
```

**Clash Verge:**
```env
SOCKS5_PROXY=127.0.0.1:7890
```

**Tor:**
```env
SOCKS5_PROXY=127.0.0.1:9050
```

## Запуск

```bash
# Разработка
go run cmd/bot/main.go

# Сборка
go build -o hysconfigbot.exe cmd/bot/main.go

# Запуск бинарника
./hysconfigbot.exe
```

## Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | 🚀 Запустить бота |
| `/goconfig` | 🔑 Создать YAML-конфиг |
| `/config` | 📁 Показать мои конфиги |
| `/help` | ❓ Справка |

## Процесс создания конфига

1. Отправьте `/goconfig`
2. Введите адрес сервера (например, `556kurumi.hs.vc`)
3. Введите имя пользователя (латиница, макс. 32 символа)
4. Получите конфиг с уникальным паролем

## Функции безопасности

- ✅ Валидация имени пользователя (только латиница и цифры)
- ✅ Валидация адреса сервера
- ✅ Лимит конфигов: 10 на пользователя
- ✅ Изоляция конфигов по chatID
- ✅ Безопасные временные файлы
- ✅ Защита от race conditions

## Пример ответа

```
Конфиг создан

alice: a3f5b8c9d2e1f4a7b6c8d9e0f1a2b3c4

[Файл: alice_config.yaml]
[Код YAML]
```